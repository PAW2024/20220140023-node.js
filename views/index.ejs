<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Persipa Store</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        /* General Styling */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
        }

        h1 {
            font-size: 3rem;
            color: #ff4500; /* Highlighted color */
            font-weight: bold;
            margin-top: 20px;
        }

        p {
            font-size: 1.2rem;
            color: #555;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
            font-size: 1rem;
            padding: 10px 20px;
            transition: all 0.3s ease-in-out;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        /* Hero Section */
        .hero {
            background-image: url('/images/images.jpeg');
            background-size: cover;
            background-position: center;
            height: 400px;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
        }

        .hero h1 {
            font-size: 3.5rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
        }

        .hero .btn {
            margin-top: 20px;
            background-color: #ff4500;
            border: none;
            font-size: 1.2rem;
            padding: 10px 30px;
        }

        .hero .btn:hover {
            background-color: #e63900;
        }

        /* Content Section */
        .content {
            margin: 40px 0;
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .card {
            background-color: white;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
            width: 300px;
        }

        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
        }

        .card img {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-top-left-radius: 10px;
            border-top-right-radius: 10px;
        }

        .card-text {
            padding: 20px;
            text-align: center;
        }

        .card-text h2 {
            font-size: 1.5rem;
            color: #333;
        }

        .card-text p {
            font-size: 1rem;
            color: #777;
        }

        .card-text .btn {
            background-color: #007bff;
            border: none;
            font-size: 1rem;
            padding: 10px 20px;
            transition: all 0.3s ease-in-out;
        }

        .card-text .btn:hover {
            background-color: #0056b3;
        }

        /* Footer */
        footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 20px;
            margin-top: 40px;
        }

        footer p {
            margin: 0;
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <div class="hero">
        <div>
            <h1>Wani Ngeyel !!</h1>
            <p>Persipa Adalah Tim Kebanggaan Anda</p>
            <a href="/contact" class="btn">Go to Contact Page</a>
        </div>
    </div>

    <!-- Content Section -->
    <div class="container">
        <div class="content">
            <!-- Awway Jersey Card -->
            <div class="card">
                <img src="/images/Awway.jpeg" alt="Awway Jersey">
                <div class="card-text">
                    <h2>Awway Jersey</h2>
                    <p>Beli sekarang juga!</p>
                    <a href="#" class="btn">Shop Now</a>
                </div>
            </div>

            <!-- Home Jersey Card -->
            <div class="card">
                <img src="/images/Home.jpeg" alt="Home Jersey">
                <div class="card-text">
                    <h2>Home Jersey</h2>
                    <p>Beli sekarang juga!</p>
                    <a href="#" class="btn">Shop Now</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 Persipa Wani Ngeyel. All Rights Reserved.</p>
    </footer>
</body>
</html>
